# Deskemoji v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Deskemoji v0.2 的完整交互功能，包括右键菜单、设置窗口和配置文件系统

**Architecture:** 新增 config、menu、settings 三个模块，使用 serde/serde_json 处理配置

**Tech Stack:** Rust, winit, tray-icon, serde, serde_json

---

## 文件结构

```
src/
├── main.rs          # 修改：集成新模块
├── monitor.rs       # 保持
├── emoji.rs         # 保持
├── renderer.rs      # 保持
├── config.rs        # 新增：配置管理
├── menu.rs          # 新增：右键菜单
└── settings.rs      # 新增：设置窗口
```

---

### Task 1: 添加依赖和配置模块

**Files:**
- Modify: `Cargo.toml`
- Create: `src/config.rs`

- [ ] **Step 1: 更新 Cargo.toml 添加依赖**

```toml
[dependencies]
# ... 现有依赖
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
```

- [ ] **Step 2: 创建 config.rs**

```rust
use serde::{Deserialize, Serialize};
use std::fs;
use std::path::PathBuf;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Config {
    pub auto_mode: bool,
    pub startup: bool,
    pub update_interval_secs: u64,
    pub cpu_threshold: f32,
    pub memory_threshold: f32,
    pub idle_threshold_secs: u64,
    pub opacity: f32,
    pub window_size: u32,
}

impl Default for Config {
    fn default() -> Self {
        Self {
            auto_mode: true,
            startup: false,
            update_interval_secs: 2,
            cpu_threshold: 80.0,
            memory_threshold: 90.0,
            idle_threshold_secs: 300,
            opacity: 1.0,
            window_size: 120,
        }
    }
}

impl Config {
    pub fn load() -> Self {
        let path = Self::config_path();
        if path.exists() {
            fs::read_to_string(&path)
                .ok()
                .and_then(|s| serde_json::from_str(&s).ok())
                .unwrap_or_default()
        } else {
            let config = Self::default();
            config.save();
            config
        }
    }

    pub fn save(&self) {
        let path = Self::config_path();
        if let Ok(json) = serde_json::to_string_pretty(self) {
            let _ = fs::write(path, json);
        }
    }

    fn config_path() -> PathBuf {
        let exe_path = std::env::current_exe().unwrap_or_default();
        exe_path.parent().unwrap_or(&PathBuf::from(".")).join("config.json")
    }
}
```

- [ ] **Step 3: 编译验证**

```bash
cargo check
```

- [ ] **Step 4: 提交**

```bash
git add Cargo.toml src/config.rs
git commit -m "feat: add config module with JSON serialization"
```

---

### Task 2: 右键菜单模块

**Files:**
- Create: `src/menu.rs`
- Modify: `src/main.rs`

- [ ] **Step 1: 创建 menu.rs**

```rust
use tray_icon::menu::{Menu, MenuItem, Submenu, MenuEvent, PredefinedMenuItem};

pub struct AppMenu {
    pub menu: Menu,
    pub auto_mode_item: MenuItem,
    pub startup_item: MenuItem,
    pub settings_item: MenuItem,
    pub quit_item: MenuItem,
    manual_emojis: Vec<MenuItem>,
}

#[derive(Debug, Clone)]
pub enum MenuAction {
    ManualSelect(String),
    ToggleAutoMode,
    ToggleStartup,
    OpenSettings,
    Quit,
}

const EMOJI_OPTIONS: &[(&str, &str)] = &[
    ("🙂", "开心"),
    ("😢", "难过"),
    ("😡", "生气"),
    ("😴", "困倦"),
    ("🤔", "思考"),
    ("🥵", "热"),
    ("💀", "崩溃"),
    ("🌙", "晚安"),
];

impl AppMenu {
    pub fn new() -> Self {
        let menu = Menu::new();
        
        // 手动选择子菜单
        let manual_menu = Submenu::new("手动选择", true);
        let mut manual_emojis = Vec::new();
        for (emoji, name) in EMOJI_OPTIONS {
            let item = MenuItem::new(format!("{} {}", emoji, name), true, None);
            manual_menu.append(&item).unwrap();
            manual_emojis.push(item);
        }
        
        menu.append(&manual_menu).unwrap();
        menu.append(&PredefinedMenuItem::separator()).unwrap();
        
        let auto_mode_item = MenuItem::new("自动模式 ✓", true, None);
        menu.append(&auto_mode_item).unwrap();
        
        menu.append(&PredefinedMenuItem::separator()).unwrap();
        
        let settings_item = MenuItem::new("设置", true, None);
        let startup_item = MenuItem::new("开机启动", true, None);
        menu.append(&settings_item).unwrap();
        menu.append(&startup_item).unwrap();
        
        menu.append(&PredefinedMenuItem::separator()).unwrap();
        
        let quit_item = MenuItem::new("退出", true, None);
        menu.append(&quit_item).unwrap();
        
        Self {
            menu,
            auto_mode_item,
            startup_item,
            settings_item,
            quit_item,
            manual_emojis,
        }
    }
    
    pub fn update_auto_mode(&self, enabled: bool) {
        self.auto_mode_item.set_text(if enabled { "自动模式 ✓" } else { "自动模式" });
    }
    
    pub fn update_startup(&self, enabled: bool) {
        self.startup_item.set_text(if enabled { "开机启动 ✓" } else { "开机启动" });
    }
    
    pub fn poll_action() -> Option<MenuAction> {
        MenuEvent::receiver().try_recv().ok().map(|event| {
            // 处理事件...
            MenuAction::Quit // 简化，实际需要匹配事件ID
        })
    }
}
```

- [ ] **Step 2: 编译验证**

- [ ] **Step 3: 提交**

---

### Task 3: 设置窗口模块

**Files:**
- Create: `src/settings.rs`

- [ ] **Step 1: 创建 settings.rs**

```rust
// 设置窗口 - 使用简单文本界面或弹出窗口
// 由于 Rust GUI 复杂性，先实现命令行方式的设置
// 后续可扩展为图形界面

use crate::config::Config;

pub fn show_settings(config: &Config) -> Config {
    println!("=== Deskemoji 设置 ===");
    println!("1. 自动模式: {}", if config.auto_mode { "开" } else { "关" });
    println!("2. 开机启动: {}", if config.startup { "开" } else { "关" });
    println!("3. 更新频率: {}秒", config.update_interval_secs);
    println!("4. CPU阈值: {}%", config.cpu_threshold);
    println!("5. 内存阈值: {}%", config.memory_threshold);
    println!("6. 空闲时间: {}秒", config.idle_threshold_secs);
    println!("7. 窗口大小: {}px", config.window_size);
    println!("\n输入编号修改，输入 q 退出:");
    
    // 简化版本 - 直接返回原配置
    config.clone()
}
```

- [ ] **Step 2: 提交**

---

### Task 4: 集成到 main.rs

**Files:**
- Modify: `src/main.rs`

- [ ] **Step 1: 添加模块声明**

```rust
mod config;
mod menu;
mod settings;

use config::Config;
use menu::{AppMenu, MenuAction};
```

- [ ] **Step 2: 修改 AppState 添加配置**

```rust
struct AppState {
    // ... 现有字段
    config: Config,
    manual_emoji: Option<char>,
}
```

- [ ] **Step 3: 修改 emoji 选择逻辑**

```rust
fn get_current_emoji(&self) -> char {
    if let Some(emoji) = self.manual_emoji {
        return emoji;
    }
    if !self.config.auto_mode {
        return self.current_emoji.emoji;
    }
    self.current_emoji.emoji
}
```

- [ ] **Step 4: 添加菜单事件处理**

```rust
fn handle_menu_action(&mut self, action: MenuAction) {
    match action {
        MenuAction::ManualSelect(emoji) => {
            self.manual_emoji = emoji.chars().next();
            self.config.auto_mode = false;
            self.config.save();
            self.need_redraw = true;
        }
        MenuAction::ToggleAutoMode => {
            self.config.auto_mode = !self.config.auto_mode;
            if self.config.auto_mode {
                self.manual_emoji = None;
            }
            self.config.save();
            self.need_redraw = true;
        }
        // ... 其他事件
    }
}
```

- [ ] **Step 5: 编译测试**

- [ ] **Step 6: 提交**

---

### Task 5: 使用配置参数

**Files:**
- Modify: `src/monitor.rs`
- Modify: `src/main.rs`

- [ ] **Step 1: 修改 Monitor 使用配置阈值**

```rust
impl Monitor {
    pub fn get_info_with_thresholds(&self, config: &Config) -> SystemInfo {
        // 使用 config 中的阈值判断状态
    }
}
```

- [ ] **Step 2: 修改更新逻辑使用配置频率**

```rust
if self.last_update.elapsed() >= Duration::from_secs(self.config.update_interval_secs) {
    // 更新逻辑
}
```

- [ ] **Step 3: 编译测试**

- [ ] **Step 4: 提交**

---

## 验证清单

- [ ] 首次运行生成 config.json
- [ ] 右键显示完整菜单
- [ ] 手动选择表情有效
- [ ] 自动模式切换有效
- [ ] 设置保存到 config.json
- [ ] 下次启动读取配置
- [ ] 窗口大小可配置
- [ ] 阈值可配置并生效
